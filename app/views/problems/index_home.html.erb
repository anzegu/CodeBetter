<p id="notice"><%= notice %></p>

<h1>Challanges</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @problems.each do |problem| %>
      <tr>
        <td><%= problem.name %></td>
        <td><%= link_to 'Solve', language_path(problem.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

